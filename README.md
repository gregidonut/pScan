# PScan

## Abstract
PScan is a nmap clone that uses sub-commands,
to execute TCP port scans on a list of hosts.  

It allows you to add, list, and delete hosts
from the list using the subcommand `hosts`, 
it then let's you execute the scan on selected
ports using the subcommand `scan`. You can specify
ports using a command-line flag.

